import React from 'react';
import Layout from '../components/Layout';
import Swap from '../components/Swap';

export default () => {
    return (
        <Layout>
            <Swap />
        </Layout>
    );
};
ß